# VideoX-Fun 开发记录

## 项目概述
基于 VideoX-Fun 框架，针对 Wan2.2-Fun-5B-Control 模型进行训练和推理开发。

## 环境配置

### 远程开发环境
- **本地**: 编辑代码（Kilo session）
- **远程**: 在 GPU 服务器上 pull 代码并运行训练/推理
- **分支**: 直接在 `main` 分支开发
- **同步方式**: 每次修改后 commit & push 到 GitHub，远程 pull

### 训练配置
- **模型**: Wan2.2-Fun-5B-Control
- **GPU**: 双卡训练（GPU 6, 7）
- **Batch size**: 每卡 1
- **Gradient accumulation**: 1
- **Learning rate**: 1e-4
- **Mixed precision**: bf16
- **Checkpoint steps**: 500
- **Validation steps**: 500

## 主要功能开发

### 1. Validation 系统实现
**目标**: 训练过程中自动生成验证视频，对比 GT、Control、Mask 和生成结果

**实现**:
- 从训练数据中随机采样
- 提取 GT 视频的首尾帧作为约束
- 使用 control video 作为控制信号
- 生成 4 面板对比视频（GT | Control | Mask | Generated）
- 支持多 GPU，每张卡独立采样

**关键代码**:
```python
# log_validation 函数
- 随机采样训练数据
- 提取首尾帧：cv2.VideoCapture 读取第一帧和最后一帧
- 准备 inpaint video：get_image_to_video_latent([first_frame], [last_frame])
- 加载 control video：get_video_to_video_latent(control_video_path)
- 提取 mask：检测 control video 中的黑色区域
- 生成视频：pipeline(...)
- 拼接保存：torch.cat([gt, control, mask, generated], dim=0)
```

**解决的问题**:
1. **随机采样**: 使用 `global_step` 作为随机种子，确保每次 validation 采样不同
2. **维度对齐**: 确保 control_video 和 inpaint_video 的空间维度一致
3. **帧数对齐**: 使用 `adjust_frames_to_4n_plus_1` 确保帧数满足 (n-1) % 4 == 0

### 2. 时序反转数据增强
**目标**: 增加训练数据多样性，避免模型只学到单一方向的运动

**实现**:
- 训练时：50% 概率同时反转 GT 和 control video 的时序
- Validation 时：50% 概率反转，首尾帧也要交换

**代码位置**:
- Dataset: `videox_fun/data/dataset_image_video.py` 中的 `temporal_reverse_prob` 参数
- Validation: `predict_v2v_control_5b_batch.py` 中的 `do_reverse` 逻辑

### 3. GT-Aware Mask 提取
**目标**: 区分 3D 高斯渲染的背景空洞和前景黑色物体

**问题**:
- 单纯检测黑色区域会把前景黑色物体（如黑色衣服、头发）也 mask 掉
- 需要利用 GT 视频区分真正的渲染空洞

**解决方案**:
```python
# 训练时
control_black = control_video.max(axis=-1) < 20  # control 中的黑色区域
gt_black = gt_video.max(axis=-1) < 20            # GT 中的黑色区域
mask = control_black & ~gt_black                 # 只在 control 黑但 GT 不黑的地方 mask

# 推理时
mask = control_video.max(axis=-1) < 20           # 直接用 control 的黑色区域
```

**代码位置**:
- Dataset: `videox_fun/data/dataset_image_video.py` 第 475-485 行
- Validation: `predict_v2v_control_5b_batch.py` 第 340-350 行

### 4. ControlMaskEncoder
**目标**: 让模型学会忽略无效的 control 区域（黑色空洞）

**架构**:
```python
class ControlMaskEncoder(nn.Module):
    def __init__(self, in_channels=1, out_channels=16, mid_channels=64):
        self.encoder = nn.Sequential(
            nn.Conv3d(1, 64, kernel_size=3, padding=1),
            nn.SiLU(),
            nn.Conv3d(64, 16, kernel_size=3, padding=1),
        )
    
    def forward(self, mask_latents):
        return self.encoder(mask_latents)
```

**训练策略**:
- Mask encoder 全量训练（不用 LoRA）
- 学习率是 LoRA 的 10 倍
- 在 control_latents 编码后应用：`control_latents = control_latents + mask_features`

**代码位置**:
- 定义: `videox_fun/models/__init__.py`
- 使用: `scripts/wan2.2_fun/train_control_lora.py` 第 2094-2106 行
- Pipeline: `videox_fun/pipeline/pipeline_wan2_2_fun_control.py` 第 722-733 行

### 5. 推理脚本批量处理
**目标**: 支持从 JSON 文件批量读取测试数据进行推理

**功能**:
- 读取测试数据目录结构
- 解析 gen_index.json 获取多段视频配置
- 从 control video 提取指定帧范围
- 多段视频生成后拼接
- 自动调整分辨率保持宽高比

**代码位置**: `examples/wan2.2_fun/predict_v2v_control_5b_batch.py`

## Bug 修复记录

### 1. DataLoader Worker OOM
**问题**: DataLoader worker 进程被 OOM Killer 终止
**原因**: mask 提取增加了内存使用
**解决**: 
- 减少 `dataloader_num_workers` 从 8 到 4
- 使用 uint8 代替 float32 存储 mask（节省 3x 内存）

### 2. 变量名遮蔽
**问题**: `AttributeError: 'int' object has no attribute 'interpolate'`
**原因**: 变量名 `F` 遮蔽了 `torch.nn.functional`
**解决**: 重命名 `F` 为 `num_frames`

### 3. 宽高比不匹配
**问题**: `RuntimeError: The size of tensor a (33) must match the size of tensor b (32)`
**原因**: GT 和 control video 宽高比不同，导致 latent 维度不匹配
**解决**: 从 control video 读取宽高比计算 target_w

### 4. Pipeline mask 维度不匹配
**问题**: `RuntimeError: Trying to create tensor with negative dimension -315`
**原因**: 5B 模型的 spatial_compression_ratio=16，当 latent 空间维度为奇数时，`mask[:, ::2, ::2]` 的 stride-2 切片会丢失元素
**解决**: 使用 `F.interpolate` 按 patch_size 精确下采样

### 5. 分辨率对齐
**问题**: `RuntimeError: shape '[1, 21, 4, 1080, 608]' is invalid`
**原因**: 1080 不是 16 的倍数
**解决**: 确保 target_h 和 target_w 都是 16 的倍数

### 6. 通道数不匹配
**问题**: `RuntimeError: expected input[1, 145, 21, 67, 37] to have 148 channels, but got 145 channels`
**原因**: 训练时 mask 是 1 通道，推理时是 4 通道
**解决**: 统一使用 4 通道 mask

### 7. Resume from checkpoint
**问题**: `ValueError: invalid literal for int() with base 10: '5500.safetensors'`
**原因**: resume_from_checkpoint 期望目录路径，但传入的是 safetensor 文件路径
**解决**: 
- 支持从 safetensor 文件直接加载
- 自动分离并加载 mask encoder 权重
- 使用 `--use_peft_lora` 时从 transformer3d 加载 LoRA 权重

## 关键配置参数

### 训练参数
```bash
--config_path="config/wan2.2/wan_civitai_5b.yaml"
--boundary_type="full"              # 5B 模型使用 full
--validation_steps=500
--checkpointing_steps=500
--validation_samples_per_gpu=2
--validation_n_frames=81
--dataloader_num_workers=4          # 避免 OOM
```

### 推理参数
```python
video_length = 81                   # 帧数
target_height = 1080                # 1080P，会自动调整为 16 的倍数
num_inference_steps = 8             # 推理步数
guidance_scale = 6.0
```

## 文件修改清单

### 核心文件
1. `scripts/wan2.2_fun/train_control_lora.py`
   - 添加 ControlMaskEncoder 初始化
   - 在训练循环中应用 mask encoder
   - 修改 checkpoint 保存逻辑（包含 mask encoder）
   - 修改 resume 逻辑（支持 safetensor 文件和 mask encoder）

2. `videox_fun/pipeline/pipeline_wan2_2_fun_control.py`
   - 添加 control_mask_encoder 参数
   - 添加 control_mask 参数到 __call__
   - 在 control_video 处理后应用 mask encoder
   - 修复 mask 维度问题（使用 F.interpolate）

3. `videox_fun/data/dataset_image_video.py`
   - 添加 temporal_reverse_prob 参数
   - 实现 GT-aware mask 提取
   - 处理不同分辨率的 GT 和 control video

4. `videox_fun/models/__init__.py`
   - 添加 ControlMaskEncoder 类定义

### 脚本文件
5. `scripts/wan2.2_fun/train_control_lora.sh`
   - 配置双卡训练（GPU 6, 7）
   - 设置 validation 和 checkpoint 步数
   - 添加 TOKENIZERS_PARALLELISM=false

6. `examples/wan2.2_fun/predict_v2v_control_5b_batch.py`
   - 实现批量推理逻辑
   - 支持多段视频生成
   - 实现 GT-aware mask 提取
   - 4 面板输出（GT | Control | Mask | Generated）

### 文档文件
7. `DEV_LOG.md`
   - 记录每次修改的内容和原因
   - 记录 bug 修复过程
   - 记录关键决策和思考过程

## 版本标签

- `v1.0-stable`: 基础训练和 validation 功能
- `v2.0-mask-aware`: Mask-Aware Control Adapter 实现
- `v2.1-gt-aware-mask`: GT-Aware mask 提取

## 待优化项

1. **推理速度**: 当前 8 步推理，可以尝试更少的步数
2. **内存优化**: 1080P 81 帧推理时内存占用较大
3. **Mask 质量**: GT-aware mask 在推理时无法使用，需要探索其他方法
4. **多段视频**: 当前多段视频是独立生成后拼接，可能存在接缝问题

## 经验总结

1. **维度对齐**: 视频生成中，所有 tensor 的维度必须严格对齐，特别是时间维度和空间维度
2. **通道数一致**: 训练和推理的通道数必须一致，否则会导致维度不匹配错误
3. **分辨率对齐**: 确保 target_h 和 target_w 都是 16 的倍数（VAE 的 spatial_compression_ratio）
4. **帧数对齐**: 确保帧数满足 (n-1) % 4 == 0（VAE 的 temporal_compression_ratio）
5. **内存管理**: DataLoader worker 数量和数据类型（uint8 vs float32）会影响内存使用
6. **Checkpoint 兼容**: 修改模型结构时，要考虑 checkpoint 的兼容性

## 参考资料

- VideoX-Fun 官方文档: https://github.com/Wan-Video/VideoX-Fun
- Wan2.2 模型文档: https://github.com/Wan-Video/Wan2.2
- Diffusers 文档: https://huggingface.co/docs/diffusers
